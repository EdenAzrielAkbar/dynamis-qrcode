<div class="min-h-screen bg-slate-50">
  <nav
    class="bg-white border-b border-slate-200 px-6 py-4 flex justify-between items-center"
  >
    <h1 class="text-2xl font-bold text-blue-600">AKyuarB</h1>
    <div class="flex items-center gap-4">
      <span class="text-sm text-slate-600 font-medium"><%= user.nama %></span>
      <a href="/logout" class="text-sm text-red-500 hover:underline">Keluar</a>
    </div>
  </nav>

  <main class="max-w-4xl mx-auto p-6 lg:p-12">
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
      <div class="flex flex-col gap-6">
        <div class="bg-white p-6 rounded-3xl shadow-sm border border-slate-200">
          <div class="flex justify-between items-center">
            <div>
              <h3 class="font-semibold text-slate-800">Status QR</h3>
              <p class="text-sm text-slate-500" id="statusText">
                <%= user.is_active === 1 ? 'Aktif' : 'Nonaktif' %>
              </p>
            </div>
            <button
              id="btnToggle"
              class="relative inline-flex items-center h-7 w-12 rounded-full transition-colors duration-200 focus:outline-none <%= user.is_active === 1 ? 'bg-blue-600' : 'bg-slate-300' %>"
            >
              <span
                id="toggleCircle"
                class="inline-block w-5 h-5 transform bg-white rounded-full transition-transform duration-200 <%= user.is_active === 1 ? 'translate-x-6' : 'translate-x-1' %>"
              ></span>
            </button>
          </div>
        </div>

        <div class="bg-white p-6 rounded-3xl shadow-sm border border-slate-200">
          <h3 class="font-semibold text-slate-800 mb-4">Link Tujuan</h3>
          <form id="linkForm" class="space-y-4">
            <input
              type="url"
              id="inputLink"
              name="link"
              value="<%= user.link_tujuan || '' %>"
              placeholder="https://google.com"
              class="w-full px-4 py-3 rounded-xl border border-slate-200 focus:ring-2 focus:ring-blue-500 focus:border-transparent outline-none transition-all"
              required
            />
            <button
              type="submit"
              id="btnSubmit"
              class="w-full py-3 bg-blue-600 text-white rounded-xl font-medium hover:bg-blue-700 transition-all flex justify-center items-center gap-2"
            >
              <span id="btnText">Simpan Perubahan</span>
              <svg
                id="spinner"
                class="hidden animate-spin h-5 w-5 text-white"
                xmlns="http://www.w3.org/2000/svg"
                fill="none"
                viewBox="0 0 24 24"
              >
                <circle
                  class="opacity-25"
                  cx="12"
                  cy="12"
                  r="10"
                  stroke="currentColor"
                  stroke-width="4"
                ></circle>
                <path
                  class="opacity-75"
                  fill="currentColor"
                  d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"
                ></path>
              </svg>
            </button>
          </form>
        </div>
      </div>

      <div
        class="bg-white p-8 rounded-3xl shadow-sm border border-slate-200 text-center"
      >
        <h3 class="text-lg font-semibold mb-6 text-slate-800">QR Code Anda</h3>
        <div class="bg-slate-100 p-4 rounded-2xl inline-block mb-6">
          <img
            src="<%= user.qr_code %>"
            alt="QR Code"
            id="qrImage"
            class="w-48 h-48 mx-auto"
          />
        </div>
        <a
          href="<%= user.qr_code %>"
          download="MyQR-AKyuarB.png"
          class="block w-full py-3 bg-slate-800 text-white rounded-xl font-medium hover:bg-slate-700 transition-all"
        >
          Unduh QR Code
        </a>
      </div>
    </div>
    <div
      class="mt-8 bg-white p-6 rounded-3xl shadow-sm border border-red-100 bg-red-50/10"
    >
      <div
        class="flex flex-col md:flex-row md:items-center justify-between gap-4"
      >
        <div>
          <h3 class="font-semibold text-red-600 flex items-center gap-2">
            <svg
              xmlns="http://www.w3.org/2000/svg"
              fill="none"
              viewBox="0 0 24 24"
              stroke-width="2"
              stroke="currentColor"
              class="w-5 h-5"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M12 9v3.75m9-.75a9 9 0 1 1-18 0 9 9 0 0 1 18 0Zm-9 3.75h.008v.008H12v-.008Z"
              />
            </svg>
            Zona Bahaya
          </h3>
          <p class="text-sm text-slate-500 mt-1">
            Menghapus akun akan menghapus profil Anda dan seluruh data tautan
            dynamic QR Code secara permanen dari sistem **AKyuarB**.
          </p>
        </div>
        <div>
          <button
            id="btnDeleteAccount"
            class="w-full md:w-auto px-5 py-2.5 bg-red-100 hover:bg-red-200 text-red-600 rounded-xl font-medium text-sm transition-all border border-red-200"
          >
            Hapus Akun Saya
          </button>
        </div>
      </div>
    </div>
  </main>
</div>

<div
  id="dashboard-data"
  class="hidden"
  data-status="<%= user.is_active %>"
  data-user-name="<%= user.nama %>"
></div>

<div
  id="toast-container"
  class="fixed bottom-5 right-5 z-50 flex flex-col gap-2"
></div>

<script src="/js/script.js"></script>
